{
  "routes": [
    {
      "method": "GET",
      "path": "/artworks",
      "handler": "artwork.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/artworks/:id",
      "handler": "artwork.findOne",
      "config": {
        "policies": []
      }
    }
  ]
}
