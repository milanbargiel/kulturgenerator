{
  "routes": [
    {
      "method": "GET",
      "path": "/artworks",
      "handler": "artwork.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/artworks/:slug",
      "handler": "artwork.findOne",
      "config": {
        "policies": []
      }
    }
  ]
}
