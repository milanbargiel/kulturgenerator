{
  "name": "kulturgenerator-participate",
  "version": "1.0.0",
  "description": "",
  "main": "Code.js",
  "scripts": {
    "eslint": "eslint './'",
    "predeploy": "clasp push",
    "deploy": "LAST_DEPLOYMENT_ID=$(clasp deployments | pcre2grep -o1 '\\- ([A-Za-z0-9\\-\\_]+) @\\d+ - web app meta-version' ) ; [ -z $LAST_DEPLOYMENT_ID ] && LAST_DEPLOYMENT_ID=$(clasp deployments | tail -1 | pcre2grep -o1 '\\- ([A-Za-z0-9\\-\\_]+)'); clasp deploy --deploymentId $LAST_DEPLOYMENT_ID"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@google/clasp": "^2.3.0",
    "eslint": "^6.8.0",
    "eslint-config-airbnb-base": "^14.1.0",
    "eslint-plugin-import": "^2.20.2"
  }
}
